import { TimelineRowMaxEffectsPipe } from './timeline-row-max-effects.pipe';

describe('TimelineRowMaxEffectsPipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineRowMaxEffectsPipe();
    expect(pipe).toBeTruthy();
  });
});
